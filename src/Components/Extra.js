import React from 'react';
// import { Card } from '@mui/material';

const Extra = (props) => {
  return (
    <React.Fragment>
      <>
        <img src={props.image} alt="logo" style={{ width: '50px' }} />
      </>
    </React.Fragment>
  );
};

export default Extra;
